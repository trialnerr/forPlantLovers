export type FeatureProps = {
  text: string;
  heading: string;
};

export type PlantProps = {
  common_name: string; 
  image_url: string; 
};

export type Organ = {
  image: string, 
  organ: string
}